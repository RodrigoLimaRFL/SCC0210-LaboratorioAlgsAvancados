#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int n; // num objetivo

    while (true)
    {
        cin >> n;

        if(n == 0)
        {
            break;
        }

        int numerador = 0;
        int denominador = 0;
        bool digitosUsados[10];
        for(int i = 0; i < 10; i++)
        {
            digitosUsados[i] = false;
        }

    }
    return 0;
}